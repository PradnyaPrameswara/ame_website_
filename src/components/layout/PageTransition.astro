<div class="page-transition" data-page-transition aria-hidden="true"><span>AME</span></div>

<style>
  .page-transition { position: fixed; inset: 0; z-index: 998; display: grid; place-items: center; color: var(--paper); background: var(--accent); pointer-events: none; visibility: hidden; transform: translateY(100%); }
  .page-transition span { font: 850 clamp(5rem, 17vw, 17rem)/.68 var(--font-display); letter-spacing: -.09em; }
</style>
