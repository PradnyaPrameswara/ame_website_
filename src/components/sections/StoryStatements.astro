<section class="story-statements" aria-label="Our story" data-story-statements>
  <div class="page-container page-container--wide">
    <article class="story-statement story-statement--left"><p>We begin with material: grain, glaze, texture, weight, and the way a surface changes in light.</p></article>
    <article class="story-statement story-statement--right"><p>Every piece is refined through use until <em>form feels inevitable.</em></p></article>
    <article class="story-statement story-statement--center"><p>Craft is not decoration. It is the care that makes ordinary rituals feel considered.</p></article>
    <article class="story-statement story-statement--wide"><p>We make objects with a point of view and <em>enough patience to make them last.</em></p></article>
  </div>
</section>

<style>
  .story-statements { overflow: clip; background: var(--white); }
  .story-statement { display: grid; min-height: 72svh; align-items: center; padding-block: 8rem; }
  .story-statement p { width: min(48rem, 58%); font: 500 clamp(2.5rem, 5.6vw, 6.3rem)/.94 var(--font-body); letter-spacing: -.07em; }
  .story-statement em { color: var(--accent); font-family: var(--font-accent); font-weight: 400; }
  .story-statement--right p { justify-self: end; }
  .story-statement--center p { width: min(50rem, 62%); justify-self: center; }
  .story-statement--wide p { width: min(70rem, 84%); }

  @media (max-width: 47.99rem) { .story-statement { min-height: 62svh; padding-block: 5rem; } .story-statement p { width: 100%; font-size: 10.5vw; } }
</style>
